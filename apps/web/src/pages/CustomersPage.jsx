import { EntityListPage } from '../features/parties/EntityListPage.jsx';
// Clientes preserva a configuração padrão do módulo compartilhado PF/PJ.
export function CustomersPage() {
  return <EntityListPage />;
}
